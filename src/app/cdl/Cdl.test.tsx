/* eslint-disable */
import React from 'react';
import { render } from '@testing-library/react';
import Cdl from './Cdl';

describe('Cdl', () => {
  test('renders learn react link', () => {
    render(<Cdl />);
  });
});
